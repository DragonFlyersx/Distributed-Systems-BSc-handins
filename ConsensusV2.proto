syntax = "proto3";

package TokenRing;

option go_package = "/TokenRing";

service Node {
    rpc SendToken (stream Token) returns (stream Token);
}

message Token {
    int32 tokenID = 1;
    int32 Leader = 2;
    string timeStamp = 3;
}