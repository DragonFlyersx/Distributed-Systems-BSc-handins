syntax = "proto3";

package time;

option go_package = "/Exercise05";

service TimeService {
    rpc GetCurrentTime(TimeRequest) returns (TimeResponse);
}

message TimeRequest {
}

message TimeResponse {
    string current_time = 1;
}